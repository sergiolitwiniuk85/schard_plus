% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_engine.R
\name{apply_filter_thresholds}
\alias{apply_filter_thresholds}
\title{Apply QC filter thresholds}
\usage{
apply_filter_thresholds(obs, thresholds = list(), qc_cols = NULL)
}
\arguments{
\item{obs}{data.frame with cell metadata (must have detected QC columns)}

\item{thresholds}{list with threshold values, e.g.:
list(pct_mito = 10, n_genes = c(500, 6000), n_UMI = c(1000, NA), doublet_score = 0.25)
Each entry can be: a single number (upper bound), or a 2-element vector c(lower, upper).
Use NA for one-sided bounds, e.g., c(500, NA) = lower bound only.}

\item{qc_cols}{optional named vector to override QC column detection}
}
\value{
list with:
\itemize{
\item pass: logical vector (TRUE = passes all thresholds)
\item fail_reason: character vector with reason for failing ("" if pass)
}
}
\description{
Apply QC filter thresholds
}
